file_name = 'timevsspeed';
fileID = fopen(strcat(file_name, '.csv'));
C = textscan(fileID, '%f,%f,%f');
fclose(fileID);
celldisp(C);


X = unique(C{1});
[m, n] = size(X);

Y = transpose(reshape(X, m, []));

M = mean(Y, 2);
E = std(Y, 1, 2);


errorbar(C{1}, M, E);
title(file_name);
xlabel('speed');
ylabel('simulation life');
